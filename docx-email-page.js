import { convertDocx } from './docx-email.js';

const BUILTIN_GROUPS = [
  ['保單與繳費', [['id', '失敗任務 ID'], ['policyId', '保單編號'], ['policyPaymentRecordId', '保單繳費記錄編號'], ['policyNo', '保單號'], ['paymentDate', '繳費日期'], ['paymentDateEn', '繳費日期（英文）'], ['netPremium', '保費'], ['premium', '保費及徵費'], ['currency', '貨幣'], ['refundBalanceAmount', '保費回贈'], ['futurePremiumDepositAmount', '保費儲備金額'], ['premiumPayable', '應繳保費'], ['premiumPayableCorrect', '保費是否正確'], ['paymentNumber', '繳費編號'], ['dda', '自動扣款 DDA'], ['paymentFrequency', '繳費頻率'], ['paymentFrequencyEn', '繳費頻率（英文）'], ['firstPaymentType', '首期付款方式'], ['restPaymentType', '續期付款方式'], ['maturityDays', '到期天數'], ['expireDays', '過期天數'], ['gracePeriodDate', '保費寬限期'], ['prepay', '預繳保費']]],
  ['投保人與受保人', [['ownerName', '投保人'], ['ownerPinyin', '投保人英文名'], ['ownerEmail', '投保人電郵'], ['ownerIdCardLastFour', '投保人身份證後四位'], ['ownerType', '投保人類型'], ['ownerPhone', '投保人電話'], ['ownerAddress', '投保人地址'], ['ownerTitle', '投保人稱謂（中文）'], ['ownerTitleEn', '投保人稱謂（英文）'], ['ownerLastnamePinyin', '投保人姓氏（英文）'], ['insuredName', '受保人名稱'], ['insuredEmail', '受保人電郵'], ['insuredPinyin', '受保人英文名'], ['insuredTitle', '受保人稱謂（中文）'], ['insuredTitleEn', '受保人稱謂（英文）']]],
  ['保險公司與產品', [['companyName', '保險公司'], ['companyNameEnglish', '保險公司英文名'], ['companyFullName', '保險公司全稱'], ['companyFullNameEnglish', '保險公司全稱（英文）'], ['productName', '產品名稱'], ['productNameEnglish', '產品名稱（英文）'], ['productYear', '產品年期'], ['newProductYear', '新產品年期'], ['sumAssured', '保額'], ['basicProductName', '主險名稱'], ['basicPremium', '主險保費'], ['basicProductYear', '主險產品年期'], ['riderInfo', '附加險資訊組']]],
  ['銷售與行政', [['accountName', '銷售帳戶名'], ['salesName', '銷售姓名'], ['salesEmail', '銷售電郵'], ['salesPhone', '銷售電話'], ['administrativeEmails', '行政電郵'], ['secondAccountName', '第二位銷售帳戶名'], ['secondSalesName', '第二位銷售姓名'], ['secondSalesEmail', '第二位銷售電郵'], ['secondSalesPhone', '第二位銷售電話'], ['secondAdministrativeEmails', '第二位行政電郵'], ['directLeaderAccount', '直屬上級帳戶'], ['directLeaderName', '直屬上級姓名'], ['directLeaderEmail', '直屬上級電郵'], ['directLeaderAdministrativeEmails', '直屬上級行政電郵'], ['directLeaderPhone', '直屬上級電話'], ['secondDirectLeaderAccount', '第二位直屬上級帳戶'], ['secondDirectLeaderName', '第二位直屬上級姓名'], ['secondDirectLeaderEmail', '第二位直屬上級電郵'], ['secondDirectLeaderAdministrativeEmails', '第二位直屬上級行政電郵'], ['secondDirectLeaderPhone', '第二位直屬上級電話']]],
  ['簽單員與通知', [['trAccountName', '簽單員帳戶'], ['trName', '簽單員姓名'], ['tr2AccountName', '簽單員 2 帳戶'], ['tr2Name', '簽單員 2 姓名'], ['tr3AccountName', '簽單員 3 帳戶'], ['tr3Name', '簽單員 3 姓名'], ['tr1Email', '簽單員 1 電郵'], ['tr2Email', '簽單員 2 電郵'], ['tr3Email', '簽單員 3 電郵'], ['tr1Phone', '簽單員 1 電話'], ['tr2Phone', '簽單員 2 電話'], ['tr3Phone', '簽單員 3 電話'], ['tr1LicenseNo', '簽單員 1 經紀牌照號碼'], ['tr2LicenseNo', '簽單員 2 經紀牌照號碼'], ['tr3LicenseNo', '簽單員 3 經紀牌照號碼'], ['skipSales', '是否跳過銷售'], ['secondSkipSales', '是否跳過第二位銷售'], ['skipLeader', '是否跳過上級'], ['secondSkipLeader', '是否跳過第二位上級'], ['skipTr1', '是否跳過簽單員 1'], ['skipTr2', '是否跳過簽單員 2'], ['skipTr3', '是否跳過簽單員 3'], ['notifyDate', '通知日期'], ['notifyDateEn', '通知日期（英文）'], ['investmentLinkedAttachmentText', 'hcyc 附件文字']]],
  ['日期與狀態', [['approvalDate', '繕發日期'], ['submissionDate', '客戶簽署日期'], ['policyDate', '保單日期'], ['signDate', '客戶簽署日期'], ['coolingOffDate', '冷靜期結束日'], ['coolingOffDateEn', '冷靜期結束日（英文）'], ['policyStatus', '保單狀態'], ['robotUpdate', '機器人是否更新'], ['toNameMap', '收件人自訂名字']]]
].map(([group, variables]) => ({ group, variables: variables.map(([field, label]) => ({ field, label })) }));
const CUSTOM_STORAGE_KEY = 'docx-email-custom-variables-v1';
const FIELD_NAME = /^[A-Za-z_][A-Za-z0-9_]*$/;
const builtinFields = new Set(BUILTIN_GROUPS.flatMap(({ variables }) => variables.map(({ field }) => field.toLowerCase())));
const $ = (id) => document.getElementById(id);
const input = $('docx-input'); const source = $('docx-source'); const preview = $('docx-preview'); const status = $('docx-status'); const error = $('docx-error'); const warnings = $('docx-warnings'); const copy = $('copy-docx-html'); const download = $('download-docx-html');
const search = $('variable-search'); const variableList = $('variable-list'); const catalogToggle = $('toggle-variable-catalog'); const customForm = $('custom-variable-form'); const customLabel = $('custom-variable-label'); const customField = $('custom-variable-field'); const customError = $('custom-variable-error'); const customList = $('custom-variable-list'); const cancelCustom = $('cancel-custom-variable'); const saveCustom = $('save-custom-variable');
let artifact = ''; let customVariables = []; let editingField = null; let catalogExpanded = false;
function useArtifact(html) { artifact = html; source.value = artifact; preview.srcdoc = artifact; copy.disabled = !artifact; download.disabled = !artifact; }
function syncSource() { artifact = source.value; preview.srcdoc = artifact; copy.disabled = !artifact; download.disabled = !artifact; }
function showFailure(message) { useArtifact(''); error.textContent = message; status.textContent = '無法轉換'; warnings.textContent = ''; }
function validCustom(variable, fields = new Set()) { return variable && typeof variable.label === 'string' && variable.label.trim() && typeof variable.field === 'string' && FIELD_NAME.test(variable.field) && !builtinFields.has(variable.field.toLowerCase()) && !fields.has(variable.field.toLowerCase()); }
function loadCustomVariables() { try { const parsed = JSON.parse(localStorage.getItem(CUSTOM_STORAGE_KEY) || '[]'); const fields = new Set(); return Array.isArray(parsed) ? parsed.filter((variable) => { if (!validCustom(variable, fields)) return false; fields.add(variable.field.toLowerCase()); return true; }).map(({ label, field }) => ({ label: label.trim(), field })) : []; } catch { return []; } }
function saveCustomVariables() { localStorage.setItem(CUSTOM_STORAGE_KEY, JSON.stringify(customVariables)); }
function insertVariable(field) { const token = `\${${field}}`; source.setRangeText(token, source.selectionStart, source.selectionEnd, 'end'); syncSource(); source.focus(); }
function makeOption(variable, kind) { const button = document.createElement('button'); button.type = 'button'; button.className = 'variable-option'; button.dataset.variableKind = kind; button.dataset.variableField = variable.field; button.append(document.createTextNode(`${variable.label} `)); const field = document.createElement('code'); field.textContent = variable.field; button.append(field); if (kind === 'builtin') { const readonly = document.createElement('span'); readonly.className = 'readonly'; readonly.textContent = '唯讀'; button.append(readonly); } button.addEventListener('click', () => insertVariable(variable.field)); return button; }
function renderCatalog() { const query = search.value.trim().toLocaleLowerCase(); const showAllBuiltins = catalogExpanded || Boolean(query); variableList.dataset.expanded = String(catalogExpanded); catalogToggle.setAttribute('aria-expanded', String(catalogExpanded)); catalogToggle.textContent = catalogExpanded ? '收合變數' : '顯示全部變數'; variableList.replaceChildren(); for (let index = 0; index < BUILTIN_GROUPS.length; index += 1) { const { group, variables } = BUILTIN_GROUPS[index]; if (!showAllBuiltins && index > 0) continue; const matches = variables.filter(({ label, field }) => !query || label.toLocaleLowerCase().includes(query) || field.toLowerCase().includes(query)); if (!matches.length) continue; const section = document.createElement('section'); section.className = 'variable-group'; const title = document.createElement('h3'); title.textContent = group; const options = document.createElement('div'); options.className = 'variable-options'; matches.forEach((variable) => options.append(makeOption(variable, 'builtin'))); section.append(title, options); variableList.append(section); } if (customVariables.length) { const matches = customVariables.filter(({ label, field }) => !query || label.toLocaleLowerCase().includes(query) || field.toLowerCase().includes(query)); if (matches.length) { const section = document.createElement('section'); section.className = 'variable-group'; const title = document.createElement('h3'); title.textContent = '自訂'; const options = document.createElement('div'); options.className = 'variable-options'; matches.forEach((variable) => options.append(makeOption(variable, 'custom'))); section.append(title, options); variableList.append(section); } } }
function resetCustomForm() { editingField = null; customForm.reset(); customError.textContent = ''; saveCustom.textContent = '新增'; cancelCustom.hidden = true; }
function renderCustomVariables() { customList.replaceChildren(); customVariables.forEach((variable) => { const row = document.createElement('div'); row.className = 'custom-variable-row'; const label = document.createElement('span'); label.textContent = variable.label; const field = document.createElement('code'); field.textContent = `\${${variable.field}}`; const edit = document.createElement('button'); edit.type = 'button'; edit.textContent = '編輯'; edit.setAttribute('aria-label', `編輯 ${variable.label}`); edit.addEventListener('click', () => { editingField = variable.field; customLabel.value = variable.label; customField.value = variable.field; saveCustom.textContent = '更新'; cancelCustom.hidden = false; customLabel.focus(); }); const remove = document.createElement('button'); remove.type = 'button'; remove.textContent = '刪除'; remove.setAttribute('aria-label', `刪除 ${variable.label}`); remove.addEventListener('click', () => { customVariables = customVariables.filter(({ field }) => field !== variable.field); saveCustomVariables(); renderCatalog(); renderCustomVariables(); resetCustomForm(); }); row.append(label, field, edit, remove); customList.append(row); }); }
source.addEventListener('input', syncSource); search.addEventListener('input', renderCatalog); catalogToggle.addEventListener('click', () => { catalogExpanded = !catalogExpanded; renderCatalog(); });
customForm.addEventListener('submit', (event) => { event.preventDefault(); const variable = { label: customLabel.value.trim(), field: customField.value.trim() }; const otherFields = new Set(customVariables.filter(({ field }) => field !== editingField).map(({ field }) => field.toLowerCase())); if (!validCustom(variable, otherFields)) { customError.textContent = '請輸入未重複的英文欄位名稱。'; return; } customVariables = editingField ? customVariables.map((item) => item.field === editingField ? variable : item) : [...customVariables, variable]; saveCustomVariables(); renderCatalog(); renderCustomVariables(); resetCustomForm(); });
cancelCustom.addEventListener('click', resetCustomForm);
customVariables = loadCustomVariables(); renderCatalog(); renderCustomVariables();
input.addEventListener('change', async () => {
  const file = input.files?.[0]; if (!file) return; error.textContent = ''; warnings.textContent = ''; status.textContent = '正在轉換…'; useArtifact('');
  try { const result = await convertDocx(file, globalThis.mammoth); useArtifact(result.html); status.textContent = '已轉換並消毒'; warnings.textContent = result.warnings.join(' '); } catch (reason) { showFailure(reason.message); }
});
copy.addEventListener('click', async () => { if (!artifact) return; try { await navigator.clipboard.writeText(artifact); status.textContent = '已複製 HTML'; } catch { source.focus(); source.select(); document.execCommand('copy'); status.textContent = '已複製 HTML'; } });
download.addEventListener('click', () => { if (!artifact) return; const url = URL.createObjectURL(new Blob([artifact], { type: 'text/html;charset=UTF-8' })); const link = document.createElement('a'); link.href = url; link.download = 'email-template.html'; link.click(); setTimeout(() => URL.revokeObjectURL(url), 1000); status.textContent = '已下載 HTML'; });
